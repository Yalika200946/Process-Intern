# Archive

Repo-root archive for non-Python assets superseded by a later version.
Python legacy modules live under `src/archive/` instead (see that folder's
own README) so they remain a valid, if unused, importable package.

- `dashboard_pro.html` — the original dashboard UI, superseded by
  `dashboard/index.html` (confirmed 2026-07 in `RUN.md`: "เวอร์ชันใหม่ แทน
  `dashboard_pro.html` เดิม"). No code references it; kept for provenance.
