# CPHT Analytics — Docs Entry Point

**Status:** CURRENT

This repo analyzes crude preheat train (CPHT) fouling, cleaning
prioritization, CIT/furnace-F101 impact, and economics for Bangchak Plant 3,
and publishes an approved dataset to a dashboard. Start here.

## Where to start

1. Read `CLAUDE.md` (repo root) for the mandatory development rules —
   they override everything else.
2. Read `CURRENT_PIPELINE_MAP.md` for what the pipeline actually does today
   (producers/consumers/outputs, verified against real code, not aspiration).
3. Read `UNRESOLVED_ENGINEERING_DECISIONS.md` for what is still
   `REQUIRES_ENGINEERING_CONFIRMATION` before you treat any number as final.

## How to run it

```
python pipeline/run_all.py                # recompute in place
python pipeline/run_all.py --input new.xlsx
python pipeline/run_all.py --only cit_forecast_export   # single notebook, for debugging
```

See `RUN.md` (repo root) for the dashboard/backend and `DEPLOY.md` for
Docker/deployment. `pipeline/run_all.py` executes the notebooks in
`notebooks/production/` in dependency order — note the run order does not
match the files' numeric prefixes exactly, see the comment at the top of
`CHAIN` in `run_all.py` and `docs/archive/MIGRATION_MAP.md` — then a chain of
`pipeline/*.py` post-processors that write `dashboard/data/*.json`.

## Where things live

- **Production notebooks:** `notebooks/production/01_*.ipynb`–`17_*.ipynb`
  (see `CURRENT_PIPELINE_MAP.md` for the exact run order, which differs
  from the numeric prefixes — see `docs/archive/MIGRATION_MAP.md`). The
  reference-only CIT-model notebook `14_cit_model_feature_matrix.ipynb`
  (merged 2026-07-19 from the former `14`/`15_cit_model_benchmark.ipynb`/
  `16_cit_shap_importance.ipynb`, 3 parts in one file) lives in `production/`
  too but is documented as "reference, not canonical" in `ARCHIVE_CANDIDATES.md`.
- **EDA notebooks:** `notebooks/eda/*.ipynb` — not called by `run_all.py`.
  Includes `00_my_analysis_walkthrough.ipynb`, a personal end-to-end
  learning notebook — not a pipeline stage, not consumed downstream.
- **Diagnostic notebooks:** `notebooks/diagnostics/*.ipynb` — verify models/
  solvers, never a source of truth for dashboard values.
- **Archived notebooks:** `notebooks/_archive_2026-07-12/`.
- **Shared engineering/modeling code:** `src/domain`, `src/features`,
  `src/models`, `src/optimization`, `src/reporting`, `src/validation` (see
  `docs/archive/MIGRATION_MAP.md` for what moved here from `notebooks/*.py`
  and why).
- **Pipeline orchestration:** `pipeline/run_all.py` (batch) and
  `backend/server.py` (dashboard's upload/recompute API).
- **Configuration:** `config/*.yaml` (limits, tag mapping, HX topology,
  data-quality rules, data contracts) — engineering limits and thresholds
  belong here, not hard-coded in notebooks.
- **Data:** external plant data lives outside the repo (`CPHT_DATA_DIR`,
  see `CURRENT_PIPELINE_MAP.md`); this repo's own `data/{raw,interim,processed}/`
  are placeholders for a future Parquet migration (currently empty,
  gitignored — the active pipeline still uses CSV, see
  `docs/archive/MIGRATION_PLAN.md` Phase 3).
- **Generated output:** `dashboard/data/*.json`, `outputs/*.csv`,
  `figures/**`, `models/*` — none of this is source; it's regenerated by
  `pipeline/run_all.py`.

## Testing

```
python -m pytest -q
```

## Top limitations right now

See `UNRESOLVED_ENGINEERING_DECISIONS.md` for the full register. Highlights:

- `Q_norm` is not one formula in this codebase (three coexisting versions —
  `Q_NORM_AUDIT.md`) and is not approved as a fouling indicator.
- E112C's topology (spare shell vs. separate position) is unresolved and
  affects two pipelines differently.
- No maintenance/cleaning log exists yet, so cleaning events are inferred,
  not confirmed (see `docs/UNRESOLVED_ENGINEERING_DECISIONS.md` §5).
- `backend/server.py` has no authentication; see §11 there.

## Canonical doc set

`docs/` was consolidated 2026-07-19 from 18 top-level files down to 9 —
several docs that separately described "what the pipeline does today" or
"which implementation is canonical" were merged into one file each, and
one-time migration bookkeeping (already-completed restructuring records)
moved to `docs/archive/`. It's grouped so you never need to open more than
one folder to know what's true today, vs. drafted, vs. aspirational:

- **`docs/` (top level) — read these, all CURRENT, all reflect the real code:**

  | Doc | Purpose |
  |---|---|
  | `CURRENT_PIPELINE_MAP.md` | what the pipeline does today — orientation/run-order narrative, stage map, dependency diagrams, and known per-file issues, all in one place |
  | `ARCHIVE_CANDIDATES.md` | what's safe to archive next, and why |
  | `SOURCE_OF_TRUTH_REGISTER.csv` (+ `SOURCE_OF_TRUTH_NOTES.md`) | which implementation is canonical per calculation, and what's explicitly *not* selected |
  | `DATA_CONTRACT_REFERENCE.md` / `CALCULATION_METHOD_REFERENCE.md` | stage contracts and formulas as implemented, including the duplicated-logic register |
  | `UNRESOLVED_ENGINEERING_DECISIONS.md` | everything blocked on an engineer's sign-off |
  | `Q_NORM_AUDIT.md` | why `Q_norm` cannot be trusted as-is |
  | `VISUAL_ANALYSIS_REQUIREMENTS.md` | plotting conventions |

- **`docs/requirements/` — REQUIRES_REVIEW, drafts awaiting sign-off, not "how it works today":** `02_Requirement_v2_SSOT.md`, `03_Business_Problem_and_Requirements.md`, and the 2 original `.docx` specs.
- **`docs/future/` — PROPOSED, explicitly deferred (see `docs/archive/MIGRATION_PLAN.md` Phase 4), skip unless you're planning the next major phase:** `TARGET_PIPELINE*.md`, `TARGET_FOLDER_STRUCTURE.md`, `TARGET_NOTEBOOK_CONTENT_PLAN.md`, `PROPOSED_CLEAN_DEPENDENCY_GRAPH.md`.
- **`docs/archive/` — HISTORICAL, superseded, kept only for history.** Includes the completed `MIGRATION_MAP.md`, `MIGRATION_PLAN.md`, `LEGACY_TO_TARGET_MAPPING.csv`, and `NOTEBOOK_DEPENDENCY_MATRIX.csv` (the last one's per-file issues are also summarized in `CURRENT_PIPELINE_MAP.md`'s appendix).
